{
  "name": "contrax-smart-contracts",
  "version": "1.0.0",
  "description": "This repository includes Solidity files for Contrax's underlying smart contracts.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && npx hardhat node"
  },
  "keywords": [
    "contrax",
    "defi",
    "yield farming",
    "auto compounding"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.22.2"
  },
  "dependencies": {
    "@uniswap/v3-core": "^1.0.1",
    "@uniswap/v3-periphery": "^1.4.3",
    "dotenv": "^16.0.1",
    "slot20": "^0.0.3"
  }
}

